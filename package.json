{
    "scripts": {
        "start": "node index.js",
        "build":"cd ai-react &&  npm run build",
        "install-client":"cd ai-react && npm install",
        "heroku-postbuild":"npm run install-client && npm run build"
      }
}